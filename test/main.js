var a = require('../app/javascripts/main.js');

var assert = require("assert");

describe('main', function(){
	it('is the most basic test you can write', function () {
		assert.equal(1, 1);
	});

	it('is a pending test');
});

